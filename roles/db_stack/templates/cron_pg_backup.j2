# /etc/cron.d/pg_backup - generated by Ansible
SHELL=/bin/bash
PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin

# Daily backup (pg_dump + gzip), retention {{ pg_backup_retention_days }} days
{{ "%02d" | format(pg_backup_cron_minute|int) }} {{ "%02d" | format(pg_backup_cron_hour|int) }} * * * root /usr/local/bin/pg_backup.sh >> /var/log/pg_backup.log 2>&1
